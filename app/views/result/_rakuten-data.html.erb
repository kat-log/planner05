<% @ranking_by_age.each do |ranking| %>
  <%= link_to image_tag(ranking['mediumImageUrls'][0]), ranking.url, target: :_blank, rel: "noopener noreferrer" %>
  <%= ranking.catchcopy %>
  <%= break if ranking.rank ==3 %>
<% end %>
